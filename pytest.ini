[pytest]
markers=
    slow: these tests are too slow to run
    superslow: these tests are super slow to run
    fistslow: first slow testcase
log_cli=true
log_level=DEBUG
log_format = %(asctime)s %(levelname)s %(message)s
log_date_format = %Y-%m-%d %H:%M:%S
log_file = logs/pytest-logs.txt